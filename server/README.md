# Hook server routes

Hook's backend lands here once the V4 contract is deployed. Until then this
placeholder holds the spot in the repo layout (mirrors `apps/xcup/server/`
and `apps/xsight/server/` slots).

Anything Hook-specific that needs server-side will be added at
`server/src/routes/hook.ts` and `server/src/services/hook*.ts` in the
[umbrella repo](https://github.com/kravadk/XSight), and referenced here.
